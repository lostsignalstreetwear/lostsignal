[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "UPS_SHIPPING_MODE,PAYPAL_ENVIRONMENT,PORT,ORDER_EMAIL_TO"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
